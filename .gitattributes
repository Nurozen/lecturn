# git autocrlf=true converts LF to CRLF on Windows, causing issues with oxfmt
* text=auto eol=lf

# Preserve upstream whitespace and blank context lines inside dependency patches.
patches/*.patch -whitespace

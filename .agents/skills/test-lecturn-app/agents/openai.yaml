interface:
  display_name: "Lecturn App Testing"
  short_description: "Launch and retain isolated Lecturn test environments"
  default_prompt: "Use $test-lecturn-app to launch an isolated Lecturn environment and iteratively test it in the browser while preserving state."
